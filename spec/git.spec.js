var fs = require('fs'),
	git_publisher = require('../lib/git.js');

describe('calling publish', function() {

	it('show retrieve git options from the config', function() {

	});

	it('return a gitty instance', function() {

	});

});
